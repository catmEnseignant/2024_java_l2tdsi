import java.util.Scanner;

public class Examen {

    public static void main(String args[]) {
    Scanner sc = new Scanner(System.in); 
    
     // Exercice3
     // Test de primalite
    /*
        int i;
        int x;
        int j;
        int estpremier;

        do {
            System.out.println("Entrer un entier strictement positif: ");
            x = sc.nextInt();
        } while (x <= 0);

        estpremier = 1;

        for (i = 2; i <= Math.sqrt(x); i++) {
            if (x % i == 0) {
                estpremier = 0;
                break;
            }
        }

        if (estpremier == 0) {
            System.out.println(x + " n'est pas un nombre premier.");

            int[] tab = new int[x]; 
            j = 0;

            for (i = 1; i <= x; i++) {
                if (x % i == 0) {
                    tab[j++] = i;
                }
            }

            System.out.print("Les diviseurs de " + x + " sont : \n");
            for (int k = 0; k < j; k++) {
                System.out.print(tab[k] + " ");
            }
        } else {
            System.out.println(x + " est un nombre premier.");
            System.out.println("Les diviseurs de " + x + " sont :  \n1, " + x);
        }
    */
    
    

      


      // Test 

  /*
        Terrain t1 = new Terrain(5.5, 2.5, "titre foncier", "SaintLoius", false);
        Terrain t2 = new Terrain(10.5, 3.5, "deliberation", "Dakar", true);

        Personne p1 = new Personne("77 777 77 77", "Mariam", "Sow");
        Personne p2 = new Personne("77 555 55 55", "Binta", "Ba");

        t1.allouer(p1);
        t2.allouer(p2);

        System.out.println(p1.toString());
        System.out.println(p2.toString());
        System.out.println(t1.toString());
        System.out.println(t2.toString());

    */


        
}
}
